import React, { Component } from 'react';

class PlayerBar extends Componet {
  render() {
    return (
      <sectipn className="player-bar">
        player bar goes here
      </section>
    );
  }
}

export default PlayerBar;
